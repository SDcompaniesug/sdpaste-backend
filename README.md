# SDPaste Backend

JSON Pastebin API for SDPaste Android App

## API Endpoints

- `POST /api/pastes` - Create new paste
- `GET /api/pastes/:id` - Get paste
- `PUT /api/pastes/:id` - Update paste
- `DELETE /api/pastes/:id` - Delete paste
- `GET /api/raw/:id` - Get raw JSON content